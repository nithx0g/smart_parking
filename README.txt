# smart_parking
# To install requirements run
pip3 install numpy easyocr imutils asyncio tk websockets paho-mqtt opencv-python datetime
#To run 
python3 sim.py

# Install mosquitto broker locally
# configuration files present in moquitto directory


